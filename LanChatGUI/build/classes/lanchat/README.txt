
1. Starting the project displays a main frame which has two buttons; "Start the Server" and "Open a client".

======= Server Side ===================================================
2. On ONE PC start the Server. This will be monitor of the chat system.
    - displays the IP of the server (Current) PC
    - shows the number of currently connected clients
    - logs the times of the connection time and leaving time of the clients.
======= Client Side ===================================================
3. CLients can be on the same PC or Other PCs on the LAN. 
4. When a CLIENT is opened from the MainFrame, a new Panel OPens asking for the IP of the server.
5. Client and Server frames, both have a back button which when pressed shows





============ bugs =====
1. closinng a client window closes all the client windows
